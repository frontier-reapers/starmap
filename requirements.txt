# Python dependencies for starmap data processing

# SQLite with precompiled bindings (for environments without _sqlite3 built-in)
pysqlite3-binary>=0.5.0

# For testing
pytest>=7.4.0
pytest-cov>=4.1.0
