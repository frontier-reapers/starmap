# Cloudflare Pages Redirects
# https://developers.cloudflare.com/pages/configuration/redirects/

# Redirect root to index.html (with system focus if provided)
/ /index.html 200
/?system=* /index.html?system=:system 200
/?route=* /index.html?route=:route 200

# Fallback for any unmatched routes - serve index.html as SPA
/* /index.html 200
